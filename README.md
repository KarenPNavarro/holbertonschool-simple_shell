Executed by @KarenPNavarro

Task 0 README, man, AUTHORS - purpose is to create the project skeleton and to set up basic shell. Authors page of the project, README with the purpose of the project and explanation of tasks, Simple shell manual page.

Task 1 Betty would be proud - Wrote a basic string utility code to pass the betty-style checker.

Task 2 Simple shell 0.1 - Write a UNIX command line interpreter.

Task 3 Simple shell 0.2 - Extended the shell to handle command arguments.

Task 4 Simple shell 0.3 -  Implemented PATH handling so the shell searches each directory in the PATH environment variable to locate executables.

Task 5 Simple shell 0.4 - Added the exit built-in command, allowing the user to terminate the shell with an optional exit status code passed as an argument.

Task 6 Simple shell 1.0 -  Implemented the env built-in command, which prints all current environment variables to standard output.